pipeline {
	agent any
	stages {
		stage('pre-reqs') {
			sh 'echo "No Pre-requisites..."'
		}
		stage('build') {
			sh 'echo "Nothing to build..."'
		}
		stage('Run') {
			sh './test_script.sh first_argument'
		}
	}
}
