pipeline {
	agent any
	stages {
		stage('pre-reqs') {
			echo "No Pre-requisites..."
		}
		stage('build') {
			echo "Nothing to build..."
		}
		stage('Run') {
			./test_script.sh first_argument
		}
	}
}
